; Supply-chain hardening: never run install/postinstall scripts from packages.
; This blocks the main npm-worm vector. If a future dependency genuinely needs
; its build script, install it explicitly with --foreground-scripts.
ignore-scripts=true
